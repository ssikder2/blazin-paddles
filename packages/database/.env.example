# Pooled connection (port 6543) — use in the app and on Vercel as DATABASE_URL.
# From Vercel Supabase: often POSTGRES_PRISMA_URL.
DATABASE_URL=""

# Direct connection (port 5432) — for `npm run db:setup` / Prisma migrate locally.
# From Vercel Supabase: often POSTGRES_URL_NON_POOLING.
# DIRECT_URL=""